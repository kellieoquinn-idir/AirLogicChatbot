# AirLogicChatbot
AirLogic Chatbot is project contains a rule‑based chatbot that provides recommendations using AQI and PM2. It uses defined logic tables to classify AQI and PM2.5 levels and generate simple, plain‑language guidance. This repository includes the core logic, example intents, and a basic app structure for running the chatbot locally. 
